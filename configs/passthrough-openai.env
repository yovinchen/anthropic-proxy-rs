# ============================================================
# OpenAI 透传配置 (使用 Auto 模式实现)
# ============================================================
# 
# 用途: 透传 OpenAI 格式请求到 OpenAI 官方 API
# 
# 注意: 当前版本没有独立的 OpenAI Passthrough 模式
#       使用 Auto 模式 + 仅配置 OpenAI 后端来实现类似效果
#
# 请求流程:
#   OpenAI 请求 (/v1/chat/completions)
#     → anthropic-proxy (透传) 
#       → OpenAI 官方 API
#
# ============================================================

# 路由模式: auto (用于实现 OpenAI 透传)
ROUTING_MODE=auto

# ============================================================
# OpenAI 后端配置 (必需)
# ============================================================

OPENAI_BASE_URL=https://api.openai.com
OPENAI_API_KEY=sk-your-openai-api-key

# ============================================================
# 可选: 同时配置 Anthropic 后端
# ============================================================
# 如果同时配置，Claude 模型请求会路由到 Anthropic

# ANTHROPIC_BASE_URL=https://api.anthropic.com
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# ============================================================
# 服务配置
# ============================================================

PORT=3000
DEBUG=false
VERBOSE=false

# ============================================================
# 测试命令
# ============================================================
#
# 1. 启动代理:
#    anthropic-proxy --config configs/passthrough-openai.env
#
# 2. 测试 OpenAI 端点 (透传到 OpenAI):
#    curl http://localhost:3000/v1/chat/completions \
#      -H "Content-Type: application/json" \
#      -H "Authorization: Bearer ignored" \
#      -d '{
#        "model": "gpt-4",
#        "messages": [{"role": "user", "content": "Hello!"}]
#      }'
#
# 3. 流式请求:
#    curl http://localhost:3000/v1/chat/completions \
#      -H "Content-Type: application/json" \
#      -d '{
#        "model": "gpt-4",
#        "stream": true,
#        "messages": [{"role": "user", "content": "Count from 1 to 5"}]
#      }'
#
# ============================================================
# 支持的端点
# ============================================================
#
# ✅ /v1/chat/completions (OpenAI 格式) - 透传到 OpenAI
# ✅ /v1/messages (Anthropic 格式) - 如果配置了 Anthropic 后端
# ✅ /health - 健康检查
#
# ============================================================
# 模型路由规则
# ============================================================
#
# gpt-*, o1-*, o3-*, openai/* → OpenAI 后端 (透传)
# claude-*, anthropic/* → Anthropic 后端 (如果配置了)
#
# ============================================================
